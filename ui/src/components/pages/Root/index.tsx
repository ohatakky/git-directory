import React, { FC, Fragment } from "react";

// todo: ロゴとデモ動画 (URLにリポジトリ名入力させる)
const Root: FC = () => {
  return (
    <Fragment>
      <Fragment>
      </Fragment>
    </Fragment>
  );
};

export default Root;
